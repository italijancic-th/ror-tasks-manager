<!DOCTYPE html>
<html>
  <head>
    <title>TaskManager<%= ' - ' + yield(:title) if content_for?(:title) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= yield(:script) %>
  </head>

  <body>

<!--    <header style="background-image: url('<%#= image_path('gradient.png') %>')">-->
    <header>
      <%#= link_to menu_access_path do %>
        <%#= image_tag 'task_manager_logo.png' %>
      <%# end %>

      <%= link_to image_tag('task_manager_logo.png'), menu_access_path %>

      <% if session[:user_id].present? %>
        <span>
          User: <%= @username %> | <%= button_to 'Logout', logout_path, {method: :delete} %>
        </span>
      <% end %>
    </header>

    <div id="content">
      <%= render(partial: 'layouts/flash') %>
      <%= yield %>
    </div>

    <footer>
      Copyright <%= Time.now.year %>
    </footer>
  </body>
</html>
